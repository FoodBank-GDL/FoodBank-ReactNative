import {
    View,
    Text
} from "react-native";
import DonationsContainer from "../../components/DonationsContainer";

const Donations = ({ }) => {

    return (
        <DonationsContainer />
    );
};

export default Donations;
