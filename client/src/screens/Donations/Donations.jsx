import {
    View,
    Text
} from "react-native";
import { Styles } from "./Styles";

const Donations = ({ }) => {

    return (
        <View style={Styles.container}>
        </View>
    );
};

export default Donations;
