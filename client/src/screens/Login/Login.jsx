import { Text, View } from "react-native";

import StatusBar from "../../components/StatusBar";

const Login = () => {
  return (
    <View>
      <StatusBar/>
      <Text>Login</Text>
    </View>
  );
};

export default Login;
